import 'package:flutter/material.dart';

const double padding = 8.0;
const double height = 316.0;

const BorderRadius borderRadius = BorderRadius.only(
  topRight: Radius.circular(20),
  topLeft: Radius.circular(20),
);
